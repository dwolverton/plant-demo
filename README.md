# Practice Assessment 6 Foo

```sql
CREATE TABLE `plant` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `name` varchar(40) DEFAULT NULL,
  `height` int DEFAULT NULL,
  PRIMARY KEY (`id`)
);


INSERT INTO `plant` (name, height) VALUES
('Flower', 3 ),
('Green Bean', 6 ),
('Weed', 1 );
```
